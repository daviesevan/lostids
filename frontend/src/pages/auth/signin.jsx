import AuthForm from "@/components/authentication/authforms";
import React from "react";

const Signin = () => {
  return (
    <div>
      <AuthForm method="login" />
    </div>
  );
};

export default Signin;
